{
  "$schema": "https://raw.githubusercontent.com/tauri-apps/tauri/dev/crates/tauri-utils/schema.json",
  "identifier": "updater-capability",
  "description": "Update check, Windows only -- tauri-plugin-updater is a Windows-only dependency (Cargo.toml), so this permission does not exist on other targets.",
  "windows": ["main", "doc-*"],
  "platforms": ["windows"],
  "permissions": ["updater:allow-check"]
}
